IMPORTANT DEPLOYMENT UPDATE:

Migrate the NeuroVault Enterprise smart contract and blockchain integration from 0G Galileo Testnet to 0G MAINNET deployment architecture.

The project must be production-ready and use 0G MAINNET for final deployment and hackathon submission.

==================================================
MAINNET REQUIREMENTS
====================

Update the entire blockchain deployment configuration to support:

* 0G Mainnet
* production-ready contract deployment
* real mainnet transaction handling
* real explorer verification

==================================================
SMART CONTRACT DEPLOYMENT
=========================

Deploy:

* AgentRegistry.sol
* all supporting contracts

to:
0G MAINNET ONLY

Requirements:

* production deployment scripts
* secure environment variable handling
* verified deployment output
* contract verification support

After deployment:
Store and display:

* mainnet contract address
* transaction hash
* explorer URL

==================================================
BLOCKCHAIN CONFIGURATION
========================

Replace all testnet-only references with:

* environment-aware configuration
* mainnet-ready RPC configuration

Create:

* production deployment config
* mainnet network configuration
* secure wallet deployment setup

==================================================
MAINNET WALLET FLOW
===================

Ensure:

* RainbowKit
* MetaMask
* wagmi

all connect correctly to:
0G MAINNET

Users should:

1. connect wallet
2. switch to 0G Mainnet automatically
3. interact with deployed production contracts

==================================================
EXPLORER INTEGRATION
====================

All blockchain interactions must generate:

* mainnet explorer links
* verifiable on-chain activity
* contract interaction visibility

Display:

* transaction history
* contract deployment status
* agent creation transactions

==================================================
AGENT ID ON MAINNET
===================

Agent IDs must:

* exist on-chain
* be queryable on mainnet
* map to real wallet ownership
* support ownership transfer

==================================================
0G STORAGE + MAINNET
====================

Ensure:

* memory uploads
* metadata storage
* encrypted memory persistence

work alongside:

* deployed mainnet Agent IDs
* wallet-linked ownership

==================================================
REMOVE TESTNET DEPENDENCY
=========================

Do NOT hardcode:

* Galileo testnet RPC
* testnet explorer links
* testnet chain IDs

Use:

* environment variables
* production-ready config management

==================================================
README UPDATE
=============

Update README to include:

* mainnet deployment section
* production deployment guide
* verified contract addresses
* mainnet explorer links
* wallet/network setup

==================================================
IMPORTANT
=========

The project should feel like:
“A real production decentralized enterprise AI infrastructure platform deployed on 0G Mainnet.”

Prioritize:

* production-grade deployment
* verifiable on-chain infrastructure
* secure wallet interaction
* real decentralized ownership
